import 'package:flutter/material.dart';

class MemberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Brook会员中心"),
      ),
      body: Text("Brook会员中心"),
    );
  }
}
